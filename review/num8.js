/* 8. Create an array composing of mixed data types: string, integer, Boolean, array. Then output each
element’s data type using for-loop. */

a = ["Tristan", 18, true, [1,2,3]]

for(let i = 0; i < a.length; i++){
    console.log(typeof(a[i]))
}
